# Airflow orchestrator dependencies (Spark jobs run on cluster)
apache-airflow==2.9.2
sqlalchemy<2.0

# Scraping & ingestion toolchain
selenium==4.27.1
beautifulsoup4==4.12.2
requests==2.31.0
psycopg2-binary==2.9.9
minio==7.1.17

# Spark client to submit work to the cluster
pyspark==3.5.4
py4j==0.10.9.7

# Search indexing
elasticsearch==8.15.1

# Validation & content processing
jsonschema==4.19.1
python-dateutil==2.8.2
PyPDF2==3.0.1
pdfplumber>=0.10.0
numpy==1.26.4
pdfminer.six>=20221105

# Embedding generation (for Silver layer + Chatbot API)
sentence-transformers==3.0.1
torch>=2.0.0

# Chatbot API (merged into Airflow container)
fastapi==0.104.1
uvicorn==0.24.0
pydantic>=2.5.0  # Use version with pre-built wheels
